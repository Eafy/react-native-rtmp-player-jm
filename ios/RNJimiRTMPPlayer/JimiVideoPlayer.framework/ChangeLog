//更新日志

1.3.5       //2019091901
1、优化在释放SDK时，偶尔之前的请求可能会导致闪退的问题；

1.3.4       //2019091601
1、修复部分BUG，提升性能；
2、优化SDK手动调用时的初始化及释放接口；
3、优化切换服务器时更新登录；

1.3.3       //2019072501
1、修复PCM音频播放器在播放低采样率音频的情况下会出现静电噪音的问题；
2、视频播放器音频流加入降噪功能，开放设置接口：-setDenoiseLevel:；
3、优化视频播放器，降低音频播放的延迟性；

1.3.2       //2019071601
1、修复日志模块JMLogUtil接口:-setSavePathDic设置路径判断字符串错误的问题；
2、修复无网络的情况下连接服务器，之后有网之后无法成功连接的问题(Hosts IP未正常解析导致)；
3、优化URL请求模块，使SDK在声明周期内一直保持与设备的心跳连接；

1.3.0       //2019062501
1、新增SDK初始化接口：JimiVideoPlayer:+Initialize，其他接口调用之前必须先初始化SDK；
2、新增SDK释放接口：JimiVideoPlayer:+DeInitialize，在准备不再使用SDK时调用此接口；
3、新增配置开发者信息静态接口：JMVideoStreamPlayer:+configWithKey:secret:token，同时移除JMVideoStreamPlayer类初始化时的开发者信息配置，在SDK初始化之后必须配置开发者信息；
4、新增配置网关服务器静态接口：JMVideoStreamPlayer:+configGatewayServer:port:timeoutInterval:；
5、新增配置Web服务器静态接口：JMVideoStreamPlayer:+configWebServer:port:heartbeatTime:；
6、新增摄像头切换接口：JMVideoStreamPlayer:-switchCamera:autoPlay:handler:，兼容以前开发者自定义时的切换摄像头接口；
7、新增消息处理中心模块：管理及处理多模块及类之前的消息传递及数据通信；
8、新增动态解析服务器域名模块，避免重复解析DNS；
9、支持JMVideoStreamPlayer类多个同时运行播放；
10、重构网关通信模块；
11、重构多线程线程池模块，包括多线程池管理模块、Htpp访问模块；
12、修复Sokcet套接字描述符未真正关闭的问题(可能导致描述符被耗尽而无法连接)；
13、修复连接失败时测试服务器的PING功能无任何打印的问题；
14、优化Socket连接时的回调状态，手动断开不再回调状态；
15、修复SDK启动之后第一次对讲失效的问题；
16、SDK内部参数AppID由之前35位修改为15位(I+12位编码+2位客户端标志位)；
17、优化SDK库，缩减库文件的大小从40M到25M(支持x86_64、armv7、arm64平台的总包大小为39M)；

1.1.9       //2019052701
1、加入消息异步处理机制，实现类模块之间的无耦合通信；
2、修复线程池多线程休眠时进行唤醒可能导致的问题；
3、修复视频录制时，部分输入音频源参数不匹配导致录制的视频没有声音的问题(对音频流参数进行重写入)；

1.1.8       //2019050601
1、修复Monitor与老SDK中进行类冲突导致无法加载的问题；
2、修复在React Native中释放时，出现PCM播放器重复释放导致的闪退问题；
3、修复在React Native中默认首次设置音量开关无效的问题；

1.1.7       //2019040901
1、删除视频录制时对系统相册权限的判断(录制到沙盒，不需要权限判断)；

1.1.6       //2019040301
1、修复对讲释放时偶尔导致的闪退问题；
2、修复直播视频播放时偶尔没有声音的问题；

1.1.5       //2019032601
1、兼容设备端使用ConcoxVideoPusher，客户端使用JimiVideoPlayer时,回放和直播可能串流的问题；

1.1.4       //2019032501
1、修复AAC音频播放器在stop之后继续play会导致闪退的问题；
2、修复SDK内部时间戳只能使用32位数据的问题；
3、修复播放异常时可能会同时多次回调状态给上层App的问题；
4、修复在释放SDK时调用stop之后进行置空nil时导致闪退的问题；
5、修复释放SDK时发送停止对讲命令闪退的问题；
6、修复Http访问池对请求句柄重复释放的问题；

1.1.3       //2019032101
1、优化播放时偶尔获取到的音视频异常导致的闪退问题；
2、修复SDK在32位机器上出现时间戳溢出导致部分问题；
3、修复线程池退出导致阻塞的问题；
4、优化其他部分细节；

1.1.2       //2019022201
1、修复SDK释放时，偶尔出现卡住导致无法退出的问题；
2、优化当App端未发送任何请求时退出，设备端无法知晓App端已退出的问题(若未发送播放请求当SDK退出时会发送SDK释放的消息给设备)；
3、修复当网络不稳定进行断开连接服务器连接或释放SDK时，偶尔出现卡住一段时间的问题；

1.1.1       //2019021301
1、修复反复初始化及释放SDK导致线程池死锁引起的闪退问题；
2、修复实时视频加载时推出时进行推出导致的闪退问题；

1.1.0       //2019012201
1、优化视频播放模块，降低网络不稳定导致的视频队列积压形成的延迟问题；
2、修复条件锁休眠1000毫秒时导致的异常问题(无法使线程正常休眠)；
3、修复线程池死锁导致网关断开无法重连等问题；
4、修复客户端停止播放后仍在发送心跳包，导致设备端不会自动停止推流的问题；
5、修复停止播放后拉流器没有被及时释放的问题；

1.0.6       //2019011101
1、优化日志模块，修复同一条日志可能打印2次的问题；
2、优化网关登录，当登录失败时可查看失败原因；
3、取消视频采集模块，防止和其他SDK冲突；
4、修复部分其他细节；

1.0.4       //2018120701
1、优化音频解码模块，支持多路音频同时解码；
2、修复音频解码过程的部分问题；

1.0.3       //2018111901
1、优化拉流及推流模块；
2、降低软解码对CPU的使用率，提高性能；

1.0.2       //2018103101
1、调整视频帧的内存大小，降低不必要的内存开销；
2、修复Socket在初始化过程进行配置或断开导致闪退的问题；

1.0.1       //2018102501
1、优化服务器连接模块；
2、修复iOS硬解码失败之后出现闪退的现象；

1.0.0       //2018101801
1、First Release；
